"use client"; import {useState} from "react"; import Link from "next/link";
export default function CardLabClient(){const [defId,setDefId]=useState(""),[iterations,setIterations]=useState(12),[result,setResult]=useState<any>(null),[busy,setBusy]=useState(false); async function run(){setBusy(true);try{const r=await fetch("/api/admin/studio/card-lab",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({defId,iterations})});setResult(await r.json());}finally{setBusy(false)}} return <main className="studio-shell min-h-screen p-8"><div className="studio-main"><div className="studio-kicker">QA // ENGINE</div><h1 className="studio-title">Automated Card Laboratory</h1><div className="studio-section mt-6 p-5"><div className="grid gap-3 md:grid-cols-[1fr_180px_auto]"><input className="input" placeholder="custom_card_def_id" value={defId} onChange={e=>setDefId(e.target.value)}/><input className="input" type="number" min={1} max={100} value={iterations} onChange={e=>setIterations(Number(e.target.value))}/><button className="btn-primary" disabled={busy||!defId} onClick={run}>{busy?"Running…":"Run Engine Lab"}</button></div>{result&&<pre className="mt-5 max-h-[520px] overflow-auto rounded-xl border border-white/10 bg-black/30 p-4 text-xs">{JSON.stringify(result,null,2)}</pre>}</div><Link href="/admin/studio/cards" className="btn-ghost mt-5 inline-flex">Card Studio</Link></div></main>}
