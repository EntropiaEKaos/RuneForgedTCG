import IdentitySettingsClient from "./IdentitySettingsClient";

export const metadata = { title: "Identity & Auth — RuneForge Studio" };

export default function Page() {
  return <IdentitySettingsClient />;
}
