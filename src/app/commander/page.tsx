import CommanderClient from "./CommanderClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Commander 4P Alpha · FORGED" };

export default function CommanderPage() {
  return <CommanderClient />;
}
