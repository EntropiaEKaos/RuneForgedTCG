import { NextRequest } from "next/server"; import {db} from "@/db"; import {adminAuditLogs} from "@/db/schema"; import {desc} from "drizzle-orm"; import {getAdminSessionContext,isAdminAuthorized,unauthorized,adminRoleAllowed} from "@/lib/admin-auth";
export const dynamic="force-dynamic"; export async function GET(req:NextRequest){if(!(await isAdminAuthorized(req)))return unauthorized();const actor=await getAdminSessionContext(req);if(!actor)return unauthorized();if(!adminRoleAllowed(actor.role,"admin"))return Response.json({ok:false,error:`Role ${actor.role} cannot view the audit log`},{status:403});const limit=Math.min(Number(req.nextUrl.searchParams.get("limit")||100),300);return Response.json({ok:true,rows:await db.select().from(adminAuditLogs).orderBy(desc(adminAuditLogs.createdAt)).limit(limit)});}
