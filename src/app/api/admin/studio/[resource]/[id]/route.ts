import { NextRequest } from "next/server";
import { db } from "@/db";
import { eq } from "drizzle-orm";
import { adminKeywords, adminEffects, adminRaces, adminClasses, adminInteractions, adminCollections, adminFxPresets, adminFxAssociations, cardCatalogMeta, adminEvents, adminPromotions, adminCardArchetypes, players, customCards, adminAuditLogs } from "@/db/schema";
import { getAdminSessionContext, isAdminAuthorized, unauthorized, adminRoleAllowed } from "@/lib/admin-auth";
import { validateContent, validateContentReferences } from "@/lib/content-pipeline";
import { recordEconomyTransaction } from "@/lib/economy-ledger";
import { levelFromXp } from "@/lib/achievements";
import { analyzeContentReverseDependencies } from "@/lib/content-impact";
export const dynamic = "force-dynamic";
const FX_PRESET_IDS = new Set(["summon-default","attack-default","damage-default","heal-default","death-default","levelup-default","poison-default","barrier-default","barrierbreak-default","frost-default","stun-default"]);
const tables = { keywords: adminKeywords, effects: adminEffects, archetypes: adminCardArchetypes, races: adminRaces, classes: adminClasses, interactions: adminInteractions, collections: adminCollections, "fx-presets": adminFxPresets, "fx-associations": adminFxAssociations, "card-meta": cardCatalogMeta, events: adminEvents, promotions: adminPromotions, players } as const;
export async function PATCH(req: NextRequest, { params }: { params: Promise<{ resource: string; id: string }> }) {
 if (!(await isAdminAuthorized(req))) return unauthorized(); const actor = await getAdminSessionContext(req); if (!actor) return unauthorized(); if (!adminRoleAllowed(actor.role, "designer")) return Response.json({ ok:false,error:`Role ${actor.role} cannot edit content`},{status:403}); const {resource,id}=await params; if(resource==="players"&&!adminRoleAllowed(actor.role,"admin")) return Response.json({ok:false,error:"Only admin can edit player profiles"},{status:403}); const table=tables[resource as keyof typeof tables]; if(!table)return Response.json({ok:false,error:"Unknown resource"},{status:404}); const body=await req.json(); const clean={...body}; delete clean.id;delete clean.createdAt;delete clean.updatedAt;
 if(resource==="players"){const allowed=["name","avatar","cardBack","title","bio","banner","status","badges","moderatorNote","gold","dust","xp","level","mmr","peakMmr","rankedWins","rankedLosses"];for(const k of Object.keys(clean))if(!allowed.includes(k))delete clean[k];for(const n of ["gold","dust","xp","level","mmr","peakMmr","rankedWins","rankedLosses"]){if(clean[n]===undefined)continue;const v=Number(clean[n]);if(!Number.isFinite(v)||!Number.isInteger(v)||v<0)return Response.json({ok:false,error:`${n} must be a non-negative integer`},{status:400});clean[n]=v;}if(clean.level!==undefined&&clean.xp===undefined)return Response.json({ok:false,error:"level is derived from xp; edit xp instead"},{status:400});const economyChanged=["gold","dust","xp"].some(n=>clean[n]!==undefined);if(economyChanged&&!String(body.economyReason||"").trim())return Response.json({ok:false,error:"economyReason is required for admin balance changes"},{status:400});clean.__economyReason=economyChanged?String(body.economyReason).trim().slice(0,200):undefined;
 }else{const allowedByResource:Record<string,string[]>={keywords:["name","description","icon","engineKeyword","behavior"],effects:["name","description","kind","schema"],archetypes:["name","description","baseType","definition"],races:["name","description","icon","region","color"],classes:["name","description","icon","color"],interactions:["name","sourceType","sourceKey","targetType","targetKey","condition","effect","priority"],collections:["name","description","symbol","banner","releaseDate","rotationDate","metadata"],"fx-presets":["name","description","renderer","intensity","durationMs","particleBudget","screenShake","targetFlashMs","soundCue"],"fx-associations":["sourcePresetId","presetId","priority"],"card-meta":["collectionId","tags","classKeys","raceKeys","notes"],events:["name","description","type","startsAt","endsAt","rules","rewards","metadata"],promotions:["name","description","type","startsAt","endsAt","conditions","offers","metadata"]};const allowed=allowedByResource[resource];if(!allowed)return Response.json({ok:false,error:"Unsupported resource"},{status:400});for(const k of Object.keys(clean))if(!allowed.includes(k))delete clean[k];if(clean.name!==undefined)clean.name=String(clean.name).slice(0,120);if(clean.description!==undefined)clean.description=String(clean.description).slice(0,1000);
  if(resource==="fx-presets"){if(clean.renderer!==undefined&&!["motion","timeline","gpu"].includes(String(clean.renderer)))return Response.json({ok:false,error:"Invalid FX renderer"},{status:400});if(clean.intensity!==undefined&&!["subtle","standard","cinematic"].includes(String(clean.intensity)))return Response.json({ok:false,error:"Invalid FX intensity"},{status:400});if(clean.screenShake===""||clean.screenShake===null)clean.screenShake=null;else if(clean.screenShake!==undefined&&!["light","medium"].includes(String(clean.screenShake)))return Response.json({ok:false,error:"Invalid FX screenShake"},{status:400});for(const [field,min,max] of [["durationMs",80,5000],["particleBudget",0,36],["targetFlashMs",0,2000]] as const){if(clean[field]===undefined)continue;if(field==="targetFlashMs"&&(clean[field]===null||clean[field]==="")){clean[field]=null;continue;}const value=Number(clean[field]);if(!Number.isInteger(value)||value<min||value>max)return Response.json({ok:false,error:`FX ${field} must be an integer between ${min} and ${max}`},{status:400});clean[field]=value;}if(clean.soundCue!==undefined)clean.soundCue=clean.soundCue?String(clean.soundCue).slice(0,80):null;}
  if(resource==="fx-associations"){if(clean.sourcePresetId!==undefined){clean.sourcePresetId=String(clean.sourcePresetId||"*").trim();if(clean.sourcePresetId!=="*"&&!FX_PRESET_IDS.has(clean.sourcePresetId))return Response.json({ok:false,error:"Invalid FX association sourcePresetId"},{status:400});}if(clean.presetId!==undefined){clean.presetId=String(clean.presetId).trim();if(!FX_PRESET_IDS.has(clean.presetId))return Response.json({ok:false,error:"Invalid FX association presetId"},{status:400});}if(clean.priority!==undefined){const p=Number(clean.priority);if(!Number.isInteger(p)||p< -1000||p>1000)return Response.json({ok:false,error:"FX association priority must be an integer between -1000 and 1000"},{status:400});clean.priority=p;}}
  if(clean.priority!==undefined&&resource!=="fx-associations"){const p=Number(clean.priority);if(!Number.isInteger(p)||p< -100000||p>100000)return Response.json({ok:false,error:"priority must be a bounded integer"},{status:400});clean.priority=p;}if(clean.collectionId!==undefined&&clean.collectionId!==null){const c=Number(clean.collectionId);if(!Number.isInteger(c)||c<1)return Response.json({ok:false,error:"collectionId must be a positive integer"},{status:400});clean.collectionId=c;}for(const field of ["startsAt","endsAt","releaseDate","rotationDate"]){if(clean[field]===undefined||clean[field]===null)continue;const d=new Date(clean[field]);if(Number.isNaN(d.getTime()))return Response.json({ok:false,error:`${field} must be a valid date`},{status:400});clean[field]=d;}}
 if(resource!=="players"&&(clean.status==="published"||clean.releaseState==="published"||clean.enabled===true||clean.status==="archived"||clean.releaseState==="archived"))return Response.json({ok:false,error:"Content state changes must go through the Content Pipeline approval workflow."},{status:409});if(resource!=="players"){delete clean.enabled;if("status" in clean)clean.status="draft";if("releaseState" in clean)clean.releaseState="draft";}clean.updatedAt=new Date();
 if(resource!=="players"){const current=await db.select().from(table).where(eq((table as any).id,Number(id))).limit(1);if(!current[0])return Response.json({ok:false,error:"Not found"},{status:404});if(resource==="card-meta"){const defId=String((current[0] as any).defId||"");const [activeCard]=await db.select({enabled:customCards.enabled}).from(customCards).where(eq(customCards.defId,defId)).limit(1);if(activeCard?.enabled)return Response.json({ok:false,error:"The launch collection of a live card is immutable. Archive the card before editing its catalog metadata."},{status:409});}const active=current[0] as any;if(active.status==="published"||active.releaseState==="published"||active.enabled===true)return Response.json({ok:false,error:"Published or active content must be archived before editing. Create a new draft/version and publish it through the approval pipeline."},{status:409});const candidate={...current[0],...clean};const validation=validateContent(resource,candidate);const refErrors=await validateContentReferences(resource as any,candidate);if(!validation.passed||refErrors.length)return Response.json({ok:false,error:"Content failed validation",validation:{...validation,errors:[...validation.errors,...refErrors],passed:false}},{status:400});}
 try{if(resource==="players"){const reason=clean.__economyReason as string|undefined;delete clean.__economyReason;const row=await db.transaction(async tx=>{const [current]=await tx.select().from(players).where(eq(players.id,Number(id))).limit(1).for("update");if(!current)return null;const next={...clean,updatedAt:new Date()} as any;if(next.xp!==undefined)next.level=levelFromXp(Number(next.xp));const [updated]=await tx.update(players).set(next).where(eq(players.id,Number(id))).returning();if(reason)for(const currency of ["gold","dust","xp"] as const){if(clean[currency]===undefined)continue;const delta=Number(clean[currency])-Number((current as any)[currency]);if(delta)await recordEconomyTransaction(tx,{playerId:current.id,currency,amount:delta,balanceAfter:Number(clean[currency]),reason:`admin_adjustment:${reason}`,referenceType:"player",referenceId:String(current.id)});}await tx.insert(adminAuditLogs).values({action:"update",resource,resourceId:Number(id),actor:actor.actorId,details:{role:actor.role,fields:Object.keys(clean),economyReason:reason||null}});return updated;});if(!row)return Response.json({ok:false,error:"Not found"},{status:404});return Response.json({ok:true,row});}const [row]=await db.update(table).set(clean as any).where(eq((table as any).id,Number(id))).returning();if(!row)return Response.json({ok:false,error:"Not found"},{status:404});await db.insert(adminAuditLogs).values({action:"update",resource,resourceId:Number(id),actor:actor.actorId,details:{role:actor.role,fields:Object.keys(clean).filter(k=>k!=="updatedAt")}});return Response.json({ok:true,row});}catch{return Response.json({ok:false,error:"Update failed"},{status:409});}
}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{resource:string;id:string}>}){if(!(await isAdminAuthorized(req)))return unauthorized();const actor=await getAdminSessionContext(req);if(!actor)return unauthorized();if(!adminRoleAllowed(actor.role,"publisher"))return Response.json({ok:false,error:`Role ${actor.role} cannot delete content`},{status:403});const {resource,id}=await params;if(resource==="players")return Response.json({ok:false,error:"Player deletion is disabled from admin studio."},{status:405});const table=tables[resource as keyof typeof tables];if(!table)return Response.json({ok:false,error:"Unknown resource"},{status:404});const current=await db.select().from(table).where(eq((table as any).id,Number(id))).limit(1);if(!current[0])return Response.json({ok:false,error:"Not found"},{status:404});if((current[0] as any).status==="published"||(current[0] as any).releaseState==="published"||(current[0] as any).enabled===true)return Response.json({ok:false,error:"Published or active content must be archived before deletion."},{status:409});if(resource!=="card-meta"){const impact=await analyzeContentReverseDependencies(resource,current[0]);if(impact.totalActiveReferences>0)return Response.json({ok:false,error:"Content has active reverse dependencies and cannot be deleted.",impact},{status:409});}await db.delete(table).where(eq((table as any).id,Number(id)));await db.insert(adminAuditLogs).values({action:"delete",resource,resourceId:Number(id),actor:actor.actorId,details:{role:actor.role}});return Response.json({ok:true});}